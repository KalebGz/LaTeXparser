% \def{a}{aa}a
% Space b/n Arguments: \if {test}
% escaped chracters inside arguments: \def{a}{a\a}
defining macros inside def first argument: \def{\def{a}{aa}}{b}
% def macro not fully defined: \def{aa} **SHould output error **
% defined macro name is extension of base macro \def{ifdog}{asv}? just to test str_compare
% define a macro, undefine, and then define again

% this is edge file